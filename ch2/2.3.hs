-- Exercise 2.3: Correct the three syntactic errors in the script below:

{-
 - N =  a `div` length xs     Capitalization & double space
 -   where
 -      a  = 10               Indentation
 -     xs = [1,2,3,4,5]
 -}

n = a `div` length xs
  where
    a = 10
    xs = [1, 2, 3, 4, 5]
